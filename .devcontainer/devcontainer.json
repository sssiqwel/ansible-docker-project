{
  "name": "Ansible Docker Lab",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-22.04",
  "features": {
    "ghcr.io/devcontainers/features/python:1": {
      "version": "3.10"
    }
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y ansible && ansible-galaxy collection install community.docker",
  "customizations": {
    "vscode": {
      "extensions": [
        "redhat.ansible",
        "ms-azuretools.vscode-docker"
      ]
    }
  }
}
